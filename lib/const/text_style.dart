import 'package:flutter/material.dart';

const LOTTO_TEXTSTYLE = TextStyle(
  color: Colors.white,
  shadows: [
    Shadow(
      blurRadius: 10.0,
      color: Colors.black,
      offset: Offset(1.0, 1.0),
    )
  ],
  fontWeight: FontWeight.bold,
  fontSize: 15.0,
);
